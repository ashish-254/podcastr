import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["thumbs.dreamstime.com", "lovely-flamingo-139.convex.cloud"],
  },
};

export default nextConfig;
